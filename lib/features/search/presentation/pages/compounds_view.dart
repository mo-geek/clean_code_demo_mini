import 'package:flutter/material.dart';

class CompoundsView extends StatelessWidget {
  const CompoundsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
